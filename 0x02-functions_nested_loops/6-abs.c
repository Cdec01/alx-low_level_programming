#include "main.h"
#include <stdio.h>

/**
 * _abs - Prints the absolute values
 * @n: parameter
 * Return: The abosolute value
 */
int _abs(int n)
{
	if (n > 0)
		return (n);
	else
		return (n * -1);
}
