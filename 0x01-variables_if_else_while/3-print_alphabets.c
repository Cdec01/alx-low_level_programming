#include <stdlib.h>
#include <stdio.h>

/**
 * main - prints the alphabets in lowercase
 * and then in uppercase
 * Return: Always 0 (Success)
 */
int main(void)
{
	char c;
	char C;

	for (c = 'a'; c <= 'z'; c++)
	{
		putchar(c);
